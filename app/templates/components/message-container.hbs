{{yield
    (hash
      message=message
      isChecked=isChecked
      isFavorited=isFavorited
    )
    (hash
      toggleCheck=(action 'toggleCheck')
      toggleFavorite=(action 'toggleFavorite')
    )
}}
